 <ul>
	 <% if !user_signed_in? %>
	    <h1><%= link_to "BitBox", root_path %> |  <%= link_to "Shop", boxes_path %> | <%= link_to "Login", new_user_session_path %> </h1>
	 <% else %>
	    <h1><%= link_to "BitBox", root_path %> | <%= link_to "Shop", boxes_path %> | <%= link_to current_user.name, user_path(current_user)%> | <%= link_to "Sign Out", destroy_user_session_path, method: :delete %> | <%= link_to "Get Started", new_user_subscription_path(current_user) %> | Cart </h1>
	    <br>
	    <br>
	 <% end %>
 </ul>